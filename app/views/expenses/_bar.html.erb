<% expense_types = expense_types.sort_by! { |k| k[:percent]}.reverse %>
<div class="bar-bg">
<% expense_types.each do |expense_type| %>
    <% logger.debug(expense_type) %>
<% end %>
<div class="clear-float"></div>
</div>

<style>
 .bar-bg {
    height: .5em;
    padding: .5em;
  }
  .bar-bg div {
      height: .5em;
      float: left;
  }
</style>